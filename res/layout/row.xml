<RelativeLayout android:layout_width="wrap_content" android:layout_height="wrap_content"
              xmlns:android="http://schemas.android.com/apk/res/android" android:id="@+id/relativeLayout"
              >
    <ImageButton android:id="@+id/btnCancel" android:layout_width="35dp" android:layout_height="35dp"
                     android:background="@drawable/nobutton" android:focusable="false" android:focusableInTouchMode="false"
                     android:text="Edit Data" android:textColor="#0099CC"
                     android:layout_centerVertical="true" android:layout_toRightOf="@+id/btnAccept"/>
    <ImageButton android:id="@+id/btnAccept" android:layout_width="35dp" android:layout_height="35dp"
                     android:background="@drawable/yesbutton" android:focusable="false" android:focusableInTouchMode="false"
                     android:text="Delete" android:textColor="#0099CC"
                     android:layout_alignTop="@+id/btnCancel" android:layout_alignParentLeft="true"
                     android:layout_centerVertical="true"/>
    <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/imageView" android:layout_toRightOf="@+id/btnCancel" android:layout_marginLeft="5dp"
            />

</RelativeLayout>